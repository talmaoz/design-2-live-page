'use strict'

const KEY_SAFE_CONTENT_LOGGED_USER = 'safe-content-logged-user'
const KEY_SAFE_CONTENT_USERS       = 'safe-content-users'

const ADDRESS_LOGIN = 'login.html'
const ADDRESS_ADMIN = 'admin.html'
const ADDRESS_USER  = 'user.html'

const NUM_MAX_PLANET_WRAPPER_RATIO = 1 //  For example, if we only have 1 planet, it will only take NUM_MAX_PLANET_WRAPPER_RATIO(%) of its parent
const NUM_PLANET_RATIO = 0.9
const NUM_PLANET_WRAPPER_PADDING_RATIO = 1 - NUM_PLANET_RATIO

